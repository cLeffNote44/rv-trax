{
  "name": "@rv-trax/db",
  "version": "1.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx --env-file=../../.env src/migrate.ts",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx --env-file=../../.env src/seed/run.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "drizzle-orm": "^0.38.0",
    "postgres": "^3.4.0",
    "@rv-trax/shared": "workspace:*",
    "@types/bcrypt": "^5.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.0",
    "tsx": "^4.19.0"
  }
}
