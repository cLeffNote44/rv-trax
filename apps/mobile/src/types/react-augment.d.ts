// ---------------------------------------------------------------------------
// React Navigation v7 compatibility
//
// This file exists as a safety net. The primary fix is the pnpm override in
// the root package.json that forces all packages to use @types/react ^18.3.0,
// preventing the Component `refs` mismatch between React 18 and 19 type defs.
// ---------------------------------------------------------------------------

export {};
